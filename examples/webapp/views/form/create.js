define(['app'], function (app) {
    app.register
            .controller('formCreateCtrl', function ($scope, userService) {
                $scope.user ={}
        
        
        
        
            })
})